@{
    var isAuthenticated = User.Identity.IsAuthenticated;
    var userFirstName = User.FindFirst("FirstName")?.Value;
    var userName = User.Identity.Name;
}

<main>
      @await Component.InvokeAsync("BackButton")

    <section class="flex justify-center w-full py-6">
        <div class="bg-white rounded-lg shadow-lg w-4/5 p-6">
            <h2 class="text-3xl text-center font-bold mb-4 ">Necesito ayuda</h2>
            <form id="help-form" method="post">
                <div class="flex flex-col md:flex-row md:justify-center">
                    <div class="w-full md:w-1/2 py-2 md:pr-2">
                        <label for="nombre" class="flex text-left">Nombre</label>
                        <input type="text" id="nombre" name="nombre"
                            class="w-full px-4 py-2 border rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 @(isAuthenticated ? "bg-gray-100 cursor-not-allowed" : "")"
                            value="@(isAuthenticated ? userFirstName : "")"   @(isAuthenticated ? "readonly" : "")/>
                    </div>
                    <div class="w-full md:w-1/2 py-2">
                        <label for="correo" class="flex text-left">Correo</label>
                        <input type="email" id="correo" name="correo"
                            class="w-full px-4 py-2 border rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500 @(isAuthenticated ? "bg-gray-100 cursor-not-allowed" : "")"
                            value="@(isAuthenticated ? userName : "")" @(isAuthenticated ? "readonly" : "")/>
                    </div>
                </div>
                <div class="flex flex-col">
                    <label for="asunto" class="flex text-left">Asunto</label>
                    <input type="text" id="asunto"
                        class="w-full px-4 py-2 border rounded-full focus:outline-none focus:ring-2 focus:ring-blue-500" />
                </div>
                <div class="flex flex-col">
                    <label for="message" class="flex text-left">Descrpción</label>
                    <textarea type="text" id="message" rows="8" cols="5"
                        class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                        </textarea>
                </div>
                <div class="flex justify-center py-4">
                    <button class="bg-[#56b6cc] hover:bg-[#13678A] transition text-white font-bold py-2 px-8 rounded-lg">
                        Enviar
                    </button>
                </div>
            </form>
        </div>
    </section>
</main>
<script src="~/js/helpQuestion.js"></script>
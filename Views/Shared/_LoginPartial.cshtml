@using Microsoft.AspNetCore.Identity;
@inject SignInManager<User> signInManager;

<ul> 
    @if (signInManager.IsSignedIn(User))
    {
        <li>
            <a href="#" asp-controller="Account" asp-action="Logout">Logout</a>
        </li>
    }
</ul>
